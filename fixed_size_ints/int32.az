public fn test_math(a: int32, b: int32)
{
    let x = -a;
    //let x = +a; // TODO support unary +

    let x = a + b;
    let x = a - b;
    let x = a * b;
    let x = a / b;
    let x = a.remainder(b);

    var y: int32 = 0;
    y += a;
    y -= a;
    y *= a;
    y /= a;

    let c = a == b;
    let c = a =/= b;
    let c = a > b;
    let c = a >= b;
    let c = a < b;
    let c = a <= b;
}

public fn main()
{
    test_math(1, 2);
}
