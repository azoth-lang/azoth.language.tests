public fn test_math(a: byte, b: byte)
{
    let x = +a;

    let x = a + b;
    let x = a - b;
    let x = a * b;
    let x = a / b;
    let x = a.remainder(b);

    var y: byte = 0;
    y += a;
    y -= a;
    y *= a;
    y /= a;

    let c = a == b;
    let c = a =/= b;
    let c = a > b;
    let c = a >= b;
    let c = a < b;
    let c = a <= b;
}

public fn main()
{
    test_math(2, 1);
}
