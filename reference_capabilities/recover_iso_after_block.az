public fn main()
{
    let book: mut Book = Book();

    {
        let loan: mut = book;
        loan.modify();
    }

    take(move book);

    book.modify(); // ERROR `id` due to flow typing
}

public class Book
{
    public fn modify(mut self) { }
}

public fn take(book: iso Book)
{
}
