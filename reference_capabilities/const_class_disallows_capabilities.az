public fn main()
{
    let a: iso Book; // ERROR cannot declare `iso` of constant class
    let b: mut Book; // ERROR cannot declare `mut` of constant class
    let d: Book;
    let e: const Book;
    let f: id Book;
}

public fn example(
    a: iso Book, // ERROR cannot declare `iso` of constant class
    b: mut Book, // ERROR cannot declare `mut` of constant class
    d: Book,
    e: const Book,
    f: id Book)
{
}

public const class Book
{
}
