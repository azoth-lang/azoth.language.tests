public fn test_math(a: nint, b: nint)
{
    let x = -a;
    let x = +a;

    let x = a + b;
    let x = a - b;
    let x = a * b;
    let x = a / b;
    let x = a.remainder(b);

    var y: nint = 0;
    y += a;
    y -= a;
    y *= a;
    y /= a;

    let c = a == b;
    let c = a =/= b;
    let c = a > b;
    let c = a >= b;
    let c = a < b;
    let c = a <= b;
}

public fn main()
{
    test_math(1, 2);
}
