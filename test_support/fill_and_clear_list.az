// exit code: 1

public fn main(output: mut Test_Output) -> byte
{
    let items: mut = new List[int]();
    foreach i: int in 1..100
    {
        items.add(i);
    }

    items.clear();

    return (items.count == 0) as byte;
}
