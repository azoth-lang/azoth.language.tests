public fn main()
{
    let book: iso Book = new Book();
    take(move book);
    // After the move, `book` has type `id Book`
    read(book); // ERROR cannot convert `id Book` to `Book`
    identify(book);
}

public class Book
{
}

public fn take(book: iso Book)
{
}

public fn read(book: Book)
{
}

public fn identify(book: id Book)
{
}
