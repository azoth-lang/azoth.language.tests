public fn main()
{
    let book: iso Book = new Book();
    let borrow: Book = book;
    take(move borrow); // ERROR can't move from a share
}

public mut class Book
{
}

public fn take(book: iso Book)
{
}
