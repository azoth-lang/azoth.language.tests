public fn main()
{
    let book: iso Book = new Book();
    take(move book);
    // TODO shouldn't this just make borrow an `id Book`?
    let borrow = book; // ERROR use of moved value
}

public mut class Book
{
}

public fn take(book: iso Book)
{
}
