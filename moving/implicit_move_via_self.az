public fn main()
{
    let book: iso Book = new Book();
    book.take();
    read(book); // ERROR is `id Book`
    identify(book);
}

public class Book
{
    public fn take(iso self)
    {
    }
}

public fn read(_: Book) { }
public fn identify(_: id Book) { }
