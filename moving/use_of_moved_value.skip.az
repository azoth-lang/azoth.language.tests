public fn main()
{
    let book: iso Book = Book();
    take(move book);
    // Since `Book` is a struct type, moving it leaves an unusable value behind
    let borrow = book; // ERROR use of moved value
}

public struct Book
{
}

public fn take(book: Book) { }
