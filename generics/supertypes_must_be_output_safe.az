public class Example[TIn in, TOut out, T, TInd ind] <:
    In[TIn],
    Out[TOut],
    Inv[T], In[T], Out[T], Ind[T],
    Ind[TInd],
    Inv[TIn], // ERROR cannot use `in` parameter in invariant position
    Inv[TOut] // ERROR cannot use `in` parameter in invariant position
{
}

public trait In[T in] { }
public trait Out[T out] { }
public trait Inv[T] { }
public trait Ind[T ind] { }
